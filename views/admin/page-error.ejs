<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Error</title>

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@300;400;600;700&display=swap" rel="stylesheet">

    <style>
        /* Reset default styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Nunito', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #f4f7fc;
        }

        .error-container {
            text-align: center;
            background-color: #fff;
            padding: 30px;
            width: 400px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .error-container h1 {
            font-size: 100px;
            color: #e74c3c;
            font-weight: 700;
            margin-bottom: 20px;
        }

        .error-container h2 {
            color: #333;
            font-size: 24px;
            margin-bottom: 20px;
        }

        .error-container p {
            color: #777;
            font-size: 16px;
            margin-bottom: 30px;
        }

        .error-btn {
            padding: 12px 24px;
            background-color: #3498db;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .error-btn:hover {
            background-color: #2980b9;
        }

        .error-btn:active {
            background-color: #1d6f9a;
        }

        /* Responsive styling */
        @media (max-width: 400px) {
            .error-container {
                width: 90%;
                padding: 20px;
            }
        }
    </style>
</head>

<body>
    <div class="error-container">
        <h1>404</h1>
        <h2>Oops! Something went wrong.</h2>
        <p>The page you are looking for doesn't exist or there was an error processing your request.</p>
        <button class="error-btn" onclick="window.location.href='/admin/dashboard'">Back to Dashboard</button>
    </div>
</body>

</html>
